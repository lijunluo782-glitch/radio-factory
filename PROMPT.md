# 文案生成 Brief

给"选题"和"脚本文案"两类创作用的固定参考。硬约束(字数、术语、时长)已经写进
`channels/*.yaml` 的 `qc` 里,生成完 `rf check` 会自动查,这份 brief 不重复写
——只写程序查不出来的软约束:调性、判断标准、写法示范。

新频道复用这份 brief 时,只替换"频道特有"那一节。

---

## 听众

6-12 岁儿童,耳朵在听,不是眼睛在看,没有画面辅助。默认孩子只听一遍,
记不住的东西等于没说。

---

## 选题:三关测试

写选题(`Topic`)之前,先问自己能不能填出这三样,填不出来就别往池子里放:

### 1. 转述句(retell)—— "啊?"测试
一句话,≤40 字,让另一个孩子听完会说"啊?真的假的?",而不是"哦,知道了"。
判断标准:念给一个不懂这个领域的成年人听,他会不会想追问"为什么"。
- 差:"国际空间站的宇航员每天要跑步"(平铺事实,没有反差)
- 好:"宇航员在天上跑步,得把自己绑在跑步机上,不然人会飘走"(反差在动作里)

### 2. 翻转句(flip)—— "怪的不是那里,是这里"
选题不能只是"外太空很神奇",要指出反常点具体反常在哪个环节。句式:
"你以为怪的是 A,其实怪的是 B"。
- 例:"你以为怪的是失重,其实怪的是——不是没有重力,是你一直在往下掉,
  只是地球也在往下'躲',所以你追不上它"

### 3. 只有耳朵能得到的东西(demo 或 asset)
两选一,不能都没有:
- `demo`:一个身体指令,孩子当场能做("现在憋住气,数到十,你会想张嘴——
  宇航员在太空里也一样要克制这个反应")
- `asset`:一段音频素材能传达文字传达不了的东西(声音本身就是证据)

三关都过不了的选题,`rf screen` 会拒,不用等人工发现。

---

## 脚本:按 segment 类型写,不要写成一段话

`rf skeleton` 已经把结构定死、并把选题的 `title/flip/retell/demo` 自动填进对应
segment。你只需要补空着的 `vo` 段(现象、反问、解释)。写的时候对照这张表:

| segment | 大致时长 | 只做这一件事,别做别的 |
|---|---|---|
| 现象 | 30-50s | 只给现象,不解释、不埋伏笔。让孩子先"看见"这件怪事 |
| 反问 | 5-15s | 抛一个问题,问完立刻停——不要自问自答 |
| (留白 2s,程序自动插,不用写) | | |
| 解释 | 不设死,跟内容走 | 只用孩子每天做的动作打比方。禁止术语前置(术语一律 150s 后才能出现) |
| 翻转 | 20-40s | 落回"怪的不是那里,是这里"的翻转句 |
| 转述句 | 15s 左右 | 就是选题里写好的那句,原样搬,不要临时改写 |
| 明日预告 | 5-15s | 每期都要,不是可选项 |

**全期上限 8 分钟(480s),不设下限。** 时长跟着内容走——一个选题三分钟能讲透就
三分钟,需要五分钟才讲得有趣就五分钟。解释段是唯一真正有弹性的地方,多出来的
时长基本都该花在这里,而不是均摊给每一段。

**长不是目的,有趣才是。** 内容讲完了就停,不要为了凑时长在结尾加"意义升华"
式的总结句(比如"记住它,下次可别小看它"这种)——孩子会觉得你在上课。宁可
短一点、脆一点,也不要拖着尾巴讲道理。

### 硬性提醒(查得到,但提前避免省一轮返工)
- 句子别超 28 字,音频稿要能一口气念完
- 数字后面必须跟身体能感觉的参照物("1亿公里"不行,"1亿公里,相当于绕地球
  250万圈"才行)
- 开场 25 秒内必须出现钩子(现象或反问),不能先讲背景
- 不确定的事直接说"我们还不知道",不许圆过去
- 声化数据(把非声音信号转成声音的)必须声明"这是翻译过来的声音",
  不能说成"那里真的是这个声音"

---

## 多角色演绎:双主持人播客,不是一个人的独白

**这一节改过方向,以前"给渣渪/气泡这类无生命物件配一句台词"的做法不用了,
不要再写。** 原因:听感测试暴露出的核心问题是"像一个人自言自语",物件
偶尔插一句嘴解决不了这个——真正的答案是让整期节目变成两个人的对话。

每个频道自己定两位主持人,写进 `channels/*.yaml` 的 `hosts:` 字段
(id、`name`、MiniMax `voice_id`、`role`)。**主持人是频道自己的身份,
换频道换人**——space 频道现在是陈果(讲解者,沉稳好奇)和多多(反应者,
替听众问"啊?真的假的?"),别的频道以后各起各的,不共用。

具体怎么分工:
- **陈果类角色(讲解者)**:负责把事情讲清楚,现象/解释里的主干内容归他
- **多多类角色(反应者)**:负责"你猜"、追问、惊讶、吐槽——原来写给旁白
  的那些"我第一次听说的时候……""这也太离谱了吧"式反应句,现在都归她说,
  不是讲解者自己反应给自己听
- 两人交替出现,`Segment.voice` 标各自的 host id,`step` 照常填结构步骤名

`Segment.voice` 字段仍然存在,理论上还能接别的角色 id(比如未来真要做
拟人对话手法),但**日常写稿默认只用这两位主持人,不要再引入第三个声音**,
"解释段:手法库"里的拟人对话/拔河式冲突手法如果要用,也优先让两位主持人
本人演绎冲突双方的立场,而不是新增一个物件角色。

`nature` 频道的 `no_anthropomorphism`(禁拟人化)红线跟这个变化无关——
主持人是人在说话,不是给动植物配音,不受这条红线限制;但如果哪天真的
要在 `nature` 频道用拟人对话手法给动植物"配音说话",那仍然会撞红线,
不能用。

---

## 听感检查:写完读一遍,不是看一遍

第一批脚本真的过了 TTS 之后,暴露的问题不是"手法选错了",是文字写得
像"文章",不像"说话"。质检能查语法层面的硬指标(句长、术语、时长),
查不出"听着像不像人在说话"——这条只能自己念出声检查。四条具体毛病:

### 1. 别把每句话都剁成同样短的碎片
28 字上限是天花板,不是目标。一连串 6-10 字的短句连着念,听起来像在念
条目清单,不是在说话。真人说话是长短交替的——一句带点铺垫,一句短促
收住,再一句稍微展开,不是每句都卡着差不多的长度。

- 差(读起来像列表):"没到。因为地球到月亮,大概38万公里。这段距离,
  相当于来回跑一万趟。"
- 好(长短交替,有呼吸感):"没到。因为地球到月亮,差不多是38万公里——
  这么说吧,相当于你来回跑同一段路,跑一万趟。"

写完数一下连续短句(≤8 字)有没有超过 3 句——超过就该合并,或者加个
"这么说吧""你猜怎么着"之类的口语连接词把它撑开,别让它读起来像条目。

### 2. 每次"你猜",都要真的等一下
预测游戏这个手法最容易犯的错:文字上写了"你猜猜看",下一句立刻自问
自答揭晓——孩子根本没有反应的空当,"互动"只是修辞,不是真的在等你。

解决办法:"你猜"之后插一个 `silence` segment(2~3 秒),再接答案。
这个停顿比任何文字都管用——留白本身就是在说"这里轮到你说话了"。

### 3. demo 不能只留在选题里,没写进脚本
选题的 `demo` 字段(比如"弹一下手指,想象那颗渣不往下掉")是三关测试
要求填的,但 `space` 频道的脚本里目前从来没真正用过——写完选题就晾在
那儿了。`space` 结构不像 `nature` 那样有专属的"模仿一下"步骤,但这不
代表 demo 不能进脚本,只是没有固定位置,得自己找个自然的点嵌进解释段:
"现在你来试试"接上 demo 指令,后面跟一个短停顿(`silence`),再往下讲。
这也是互动,而且比"你猜"更实在——是真要求孩子动一下身体,不是问句空话。

### 4. 别用"所以"开头总结
"所以……""因此……"这种开头是老师在总结板书的语气,不是朋友在跟你
聊八卦。解释段能不能不用"所以"起头,换成一个反问、一个"结果呢",或者
干脆直接说结果,不铺垫因果链。

同时留意人称:钩子部分用"你"代入,一进解释段就切回"他/它"讲事实,
这个人称切换本身就会让人觉得"从聊天模式切到上课模式"了。尽量让"你"
贯穿全篇,不只是开场的装饰。

---

## 开场白:手写,不套死模板

`rf skeleton` 不再自动灌入固定开场白,`announce` 段留空,跟其他 vo 段一样
需要编导手写。四条底线,不管怎么写不能丢:

1. **节目名必须出现**,频道名可有可无——节目名本身够 specific,不用靠频道
   名兜底
2. **今天星期几必须点明**,措辞随意——"星期三""礼拜三""一周过了一半"
   都行,不必是"今天是星期X"这句固定播报腔
3. **内容那句要带轻钩子**(问句/半句留白/"今天要拆穿……"),别写成
   "今天讲 XX 主题"式的通知;但也别把"现象"段要揭晓的内容剧透掉——
   开场白的钩子要轻,真正展开留给现象段
4. **两位主持人都要报名字**——这是播客形式,不是一个人的独白。频道自己的
   `hosts`(见 `channels/*.yaml`)定了是谁,开场白拆成两句话,一人一句,
   `voice` 字段分别标各自的 host id,不要挤在一句话里读两个名字

示例(space 频道,天上的日子,周一,陈果 + 多多):
- 陈果:"礼拜一,天上的日子到点了,我是陈果。今天想问你一件事:宇航员的
  面包都去哪儿了?"
- 多多:"我是多多,老实说我也很想知道答案!"

`channels/*.yaml` 里 `slots[].announce` / `programs[].tagline` 还留着,是
`rf rundown` / dashboard 排播预览用的参考文案,不是强制播出稿。

---

## 结尾:也要双主播,别转述句一说完就硬切预告

开场白拆成两人说,结尾不能只剩一个人。转述句念完直接蹦一句"明天我们……",
听感很像突然被人挂了电话。结尾至少三拍,两位主持人轮流说完:

1. **收一句**——陈果类角色说"好啦,今天就聊到这儿"这种收尾话,给内容一个
   落点,不是戛然而止
2. **应一句**——多多类角色接一句(比如提醒"记得讲给朋友听",或者纯情绪
   反应),不是陈果自己把话说完
3. **预告 + 再见放一起**——这才是 `preview` 段真正的内容,谁来说都行,
   最后一句话把"明天聊什么"和"再见"合在一起收尾,不要预告完了还留一句
   干巴巴的"拜拜"

示例(接 001 面包渣那期):
- 陈果:"好啦,今天就聊到这儿。"
- 多多:"今天学到的,记得讲给朋友听!"
- 陈果:"明天,我们当一次实验室的破坏王。"
- 多多:"声音在没有空气的地方,还跑得动吗?我们明天见!"

前两句 `kind` 用 `vo`(不是真正的"预告"内容),后两句才标 `preview`。

---

## 解释段:手法库(按选题类型选,不要固定用一种)

解释段是九段结构里唯一没被锁死的自由段、也是时长弹性主要落在的地方——
现象/反问/翻转句式短,
边界已经被上面的表格框死;转述句直接搬选题里的 retell,不许改写;
声音标识/明日预告本来就是固定格式,开场白改成每期手写(见上方「开场白」)。
**只有解释段和开场白真正考验编导功力,也最容易偷懒:一种手法顺手了,
就每期都套。**

写之前先判断这条选题属于哪种类型,照下表挑对应手法,不是当天想到什么写什么:

| 手法 | 适用场景 | 怎么用 |
|---|---|---|
| 预测游戏 | 违反直觉的单点事实,没有长因果链 | 先让孩子猜"会/不会",猜完再揭晓,不铺推理过程 |
| 熟悉↔陌生对照 | 抽象概念,需要一个熟悉的参照物撑住 | "在熟悉的地方是 A,在陌生的地方却是 B",两边轮流讲。"陌生的地方"是谁,每个频道自己在"频道特有"一节里定(space 是太空,别的频道换别的) |
| 数字/尺度反差夸张 | 选题本身靠数字大到离谱制造惊讶 | 把大数字换算成孩子能想象的东西,呼应上面"数字后必须跟参照物" |
| 拟人对话/拔河式冲突 | 两种力、两种状态互相较劲 | 把物理量拟人成两个角色在打架 |
| "如果你在场"沉浸代入 | 历史事件、关键几分钟这类叙事型选题 | 用第二人称把孩子直接放进历史现场 |
| 物件拆解法 | 工程/结构类选题 | 像拆玩具一样一层层拆,每拆一层讲一个原理 |
| 五感置换法 | 声音、光这类感官本身是主角的选题 | 把一种感官的失灵换算成孩子熟悉的另一种感官失灵 |

不管用哪种手法,底线不变:孩子要能直接参与(你猜 / 你做 / 你说),
要有生活物件当锚点,要有留白节奏。**手法只是外壳,参与感是语法。**

写完一期顺手记一下这期用了哪种手法,避免连续几期撞同一个——先靠人工留意就够,
如果后面发现常年就固定那两三种、容易忘记轮换,再考虑要不要在 `Topic` 或
`Script` 里加一个 `device` 字段做提醒,现在不用做。

**这 7 种是通用手法,覆盖不了某个频道的特有内容形态时,直接在该频道自己的
"频道特有"一节里加,不要改这张通用表**——跟 `qc/rules.py` 里新增频道专属
质检规则是同一个原则(别处不用改)。比如美食文化频道大概率需要一个"发酵/
等待魔法"(时间把普通东西变成不一样的东西);动植物频道可能需要"拟态/伪装
揭穿"(看起来是 A,其实是在装 A)。这些是频道自己的资产,不进通用表。

---

## 频道特有(space:小小航天队)

- "熟悉↔陌生对照"手法里,陌生的一极固定是**太空/太空舱**,熟悉的一极是孩子的家
- 禁用术语(150s 前不许出现):微重力、对流、表面张力、比冲、烧蚀、多普勒、
  洛希极限
- 不写事故伤亡细节,不涉政治冲突和太空军事化
- 动手环节不涉及火、化学品、高处、小零件
- 机构徽标不碰,不暗示背书

---

## 产出格式

选题写成 `Topic` JSON,追加进 `data/topics/{channel}.json`(是个列表):

```json
{
  "id": "space-eat-001",
  "channel": "space",
  "program": "sky_life",
  "category": "eat",
  "title": "宇航员为什么不能带面包上天?",
  "retell": "面包在天上会掉渣,渣渣飘起来会被人吸进去,所以宇航员不能带面包",
  "flip": "你以为怪的是没有面包吃,其实怪的是——連面包渣都是危险品",
  "demo": "捏一下手边的面包屑,想象它不往下掉、飘在你眼前的样子",
  "asset": "none",
  "status": "pool",
  "sources": []
}
```

脚本永远先 `rf skeleton` 生成骨架,再补空着的 vo 段文本,不要手写整个结构。
